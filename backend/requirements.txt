# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.2.1
python-dotenv==1.0.1

# ── Agent orchestration ────────────────────────────────────────────────────────
langgraph==0.1.14
langchain-core==0.2.22

# ── LLM providers ─────────────────────────────────────────────────────────────
openai==1.30.5

# ── Document ingestion ────────────────────────────────────────────────────────
pymupdf==1.24.5          # PDF  (.pdf)
python-docx==1.1.2       # Word (.docx / .doc)

# ── RAG / Retrieval ───────────────────────────────────────────────────────────
faiss-cpu==1.8.0
sentence-transformers==3.0.0

# ── Database ──────────────────────────────────────────────────────────────────
supabase==2.5.0

# ── Fine-tuning (GPU machine — RTX 4060 / H200) ───────────────────────────────
# Install with:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install transformers peft trl datasets bitsandbytes accelerate
#
# Pinned versions validated on RTX 4060 + CUDA 12.1 + Python 3.11
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.6
datasets>=2.18.0
bitsandbytes>=0.43.0        # 4-bit QLoRA quantisation — requires CUDA
accelerate>=0.29.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
httpx==0.27.0
